# This ConfigMap is generated by deploy-monitoring.sh using:
#   kubectl create configmap kates-grafana-dashboards \
#     --from-file=config/monitoring/kates-benchmark-dashboard.json \
#     --from-file=config/monitoring/kates-phase-dashboard.json \
#     --from-file=config/monitoring/kates-trend-dashboard.json \
#     --namespace monitoring --dry-run=client -o yaml | kubectl apply -f -
#
# The grafana_dashboard label is added via kubectl label so the
# Grafana sidecar auto-discovers and loads the dashboards.
