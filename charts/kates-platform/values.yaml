kafka-cluster:
  enabled: true

kates:
  enabled: true

# Apicurio Registry (KafkaSQL storage against the kafka-cluster subchart).
# Disabled by default — enable to include the schema registry in the platform.
apicurio-registry:
  enabled: false

# Image used by the helm test connectivity pod (needs `nc`)
test:
  image: busybox:1.37
