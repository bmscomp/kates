{{- include "kafka-common.secretSync" (dict "fullname" "harness" "namespace" "dst" "labels" (include "kafka-common.selectorLabels" .) "secrets" .Values.secretSync.secrets "image" "ghcr.io/bmscomp/kates-tester:1.22.0" "watch" .Values.secretSync.watch) }}
