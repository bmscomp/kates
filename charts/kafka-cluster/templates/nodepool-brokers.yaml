{{- range .Values.brokerPools }}
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: {{ .name }}
  namespace: {{ include "kafka-cluster.namespace" $ }}
  labels:
    {{- include "kafka-cluster.labels" $ | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
spec:
  replicas: {{ .replicas }}
  roles:
    - broker
  storage:
    type: jbod
    volumes:
      - id: 0
        type: persistent-claim
        size: {{ .storageSize }}
        {{- if .storageClass }}
        class: {{ .storageClass }}
        {{- end }}
        deleteClaim: {{ $.Values.brokerDefaults.deleteClaim }}
  jvmOptions:
    {{- toYaml $.Values.brokerDefaults.jvmOptions | nindent 4 }}
  resources:
    {{- toYaml $.Values.brokerDefaults.resources | nindent 4 }}
  template:
    pod:
      {{- if .zone }}
      metadata:
        labels:
          zone: {{ .zone }}
      {{- end }}
      securityContext:
        {{- include "kafka-cluster.podSecurityContext" $ | nindent 8 }}
      {{- if $.Values.brokerDefaults.topologySpreadConstraints.enabled }}
      topologySpreadConstraints:
        - maxSkew: {{ $.Values.brokerDefaults.topologySpreadConstraints.maxSkew }}
          topologyKey: {{ $.Values.brokerDefaults.topologySpreadConstraints.topologyKey }}
          whenUnsatisfiable: {{ $.Values.brokerDefaults.topologySpreadConstraints.whenUnsatisfiable }}
          labelSelector:
            matchLabels:
              strimzi.io/pool-name: {{ .name }}
      {{- end }}
      affinity:
        {{- if .zone }}
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values:
                      - {{ .zone }}
        {{- end }}
        {{- if $.Values.brokerDefaults.podAntiAffinity.enabled }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    strimzi.io/pool-name: {{ .name }}
                topologyKey: {{ $.Values.brokerDefaults.podAntiAffinity.topologyKey }}
        {{- end }}
      {{- with $.Values.brokerDefaults.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if $.Values.brokerDefaults.priorityClassName }}
      priorityClassName: {{ $.Values.brokerDefaults.priorityClassName }}
      {{- end }}
      {{- if $.Values.brokerDefaults.sysctl.enabled }}
      initContainers:
        - name: sysctl-tuner
          image: {{ $.Values.brokerDefaults.sysctl.image }}
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              {{- range $key, $value := $.Values.brokerDefaults.sysctl.params }}
              sysctl -w {{ $key }}={{ $value }}
              {{- end }}
          securityContext:
            privileged: true
            runAsUser: 0
      {{- end }}
    kafkaContainer:
      securityContext:
        {{- include "kafka-cluster.containerSecurityContext" $ | nindent 8 }}
{{- end }}

