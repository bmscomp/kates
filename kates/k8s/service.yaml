apiVersion: v1
kind: Service
metadata:
  name: kates
  namespace: kates
  labels:
    app: kates
    app.kubernetes.io/name: kates
    app.kubernetes.io/part-of: klster
spec:
  type: ClusterIP
  selector:
    app: kates
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
